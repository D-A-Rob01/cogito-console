# v0.3 recorded experiment: SmolLM2 measured inference

Recorded: 2026-07-13 (America/New_York)

## Question

Can Cogito Console produce a complete, locally measured inference trace on Astræos without Colibrì, a paid API, or a large model?

## Reproducible setup

- Prompt: `Name one practical benefit of measuring real token probabilities.`
- Provider: `transformers`
- Model: `HuggingFaceTB/SmolLM2-360M-Instruct`
- Model revision: `a10cc1512eabd3dde888204e902eca88bddb4951`
- Tokenizer revision: `a10cc1512eabd3dde888204e902eca88bddb4951`
- Dtype / quantization / device: `float32` / `none` / `cpu`
- Greedy decoding, seed `1337`, maximum 6 new tokens, 5 alternatives
- Run fingerprint: `6d6ec911d7cff3748e71`
- Cogito run: `b9f981a409ba49d3b093f1c08156e965`

Environment:

- Windows 11 Home `10.0.26200`
- Intel Core Ultra 7 256V, 8 cores / 8 logical processors
- 16,811,347,968 bytes physical RAM
- Python `3.13.14`
- PyTorch `2.13.0+cpu`
- Transformers `5.13.1`
- psutil `7.2.2`

## Observed result

The warm run completed six tokens: `Measuring real token probabilities can help`.

The first decision emitted token ID `44642` (`Measuring`) with:

- selected-token logprob: `-1.2815643548965454` (`REAL`);
- selected-token probability: `0.27760269147078426` (`DERIVED` from `exp(logprob)`);
- full-vocabulary entropy: `4.107813835144043` bits (`DERIVED`);
- top-1/top-2 probability margin: `0.06366369128227234` (`DERIVED`);
- token latency: `1106.262000001152` ms (`REAL`);
- time to first token: `1107.6871999975992` ms (`REAL`);
- process RSS: `1,762,897,920` bytes (`REAL`);
- 33 layer summaries, from embedding output through final layer;
- layer 0 last-position L2 norm: `3.86954665184021` (`DERIVED` from a real hidden vector);
- layer 32 last-position L2 norm: `68.20577239990234` (`DERIVED` from a real hidden vector).

The full run took `4278.895300005388` ms at `1.4022310852038014` tokens/second. Its persisted JSONL export contained 229 normalized events: 198 layer summaries, 6 resource samples, 6 sampled-token events, 6 committed-token events, prompt/prefill lifecycle events, capability and completion records, plus explicit unavailable measurements.

## Steering proof

A separate browser run selected the rank-1 alternate ` decision` at token 7. Cogito cancelled run `cb338464…`, created run `45ad7dae…`, preserved seven prefix tokens, recorded zero invalidated suffix tokens, and recomputed from token 8. The intervention ledger labeled the operation `REAL branch replay`; it did not reuse the demo's narrative splice claim.

## Interpretation and caveats

This proves the v0.3 path on the named machine and revisions. Timing and resource values are observations, not performance guarantees. A cold process measured a much larger TTFT because model loading and cache initialization are included. Hidden tensors and full logits were used transiently and discarded; the retained norms, deltas, entropy, probability, and margin are derived summaries. Attention, normalized KV-cache size, and MoE routing remained explicitly unavailable.
